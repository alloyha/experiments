# docker-compose.override.yml
# Customizações locais (não versionado)
# Use este arquivo para configurações específicas do seu ambiente

version: '3.8'

services:
  postgres:
    # Descomente para expor porta localmente
    # ports:
    #   - "5432:5432"
    
    # Descomente para volumes persistentes
    # volumes:
    #   - ./data/postgres:/var/lib/postgresql/data

  kafka:
    # Environment customizado
    # environment:
    #   KAFKA_LOG_CLEANUP_POLICY: compact
    #   KAFKA_LOG_RETENTION_MS: 604800000  # 7 dias

  debezium:
    # Aumentar recursos se necessário
    # mem_limit: 1g

  minio:
    # Descomente se quiser usar MinIO em modo standalone
    # command: server /data

  dbt-flink:
    # Aumentar paralelismo
    # environment:
    #   FLINK_PROPERTIES: |
    #     taskmanager.numberOfTaskSlots: 8
